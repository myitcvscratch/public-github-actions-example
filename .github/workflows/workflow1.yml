name: Workflow 1
"on":
  - push
  - pull_request
jobs:
  workflow1_job1:
    runs-on: windows-latest
    defaults:
      run:
        shell: bash
    steps:
      - run: |-
          pwd
          cd d:/
          ls
